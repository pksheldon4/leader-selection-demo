spring:
  application:
    name: leader_selection

leaderSelection:
  enabled: false
  fixedDelay: ${LEADER_FIXED_DELAY:5000}
  initialDelay:  ${LEADER_INITIAL_DELAY:5000}
  heartBeatThreshold: ${LEADER_HEARTBEAT_THRESHOLD:30000}

someOtherProcessor:
  shouldProcess: true
  fixedDelay: ${OTHER_FIXED_DELAY:2000}
  initialDelay:  ${OTHER_INITIAL_DELAY:5000}